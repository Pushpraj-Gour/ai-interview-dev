{
    "version": "0.2.0",
    "configurations": [
		{
			"env": {
				// "ROUTERS": "projects"
				"ROUTERS": "all"
			},
            "name": "FastAPI - projects",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "app.main:app",
                "--reload",
                "--port",
                "8081"
            ],
            "jinja": true,
            "cwd": "${workspaceFolder}"
        },
		{
			"name": "Python Debugger: Current File in test",
			"type": "debugpy",
			"request": "launch",
			"program": "${file}",
			"console": "integratedTerminal",
			"cwd": "${workspaceFolder}/testing"
		}
    ]
}